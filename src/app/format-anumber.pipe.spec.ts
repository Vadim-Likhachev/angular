import { FormatANumberPipe } from './format-anumber.pipe';

describe('FormatANumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatANumberPipe();
    expect(pipe).toBeTruthy();
  });
});
